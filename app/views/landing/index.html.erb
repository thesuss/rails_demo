<h1>List of articles if DB is populated:</h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <strong><%= link_to article.title, article_path(article)%></strong><br />
      <%= article.content %>
    </li>
  <% end %>
</ul>
