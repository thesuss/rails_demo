<h1>List of articles if DB is populated:</h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <strong><a href="<%= article.id %>/ <%=article_path%>"><%= article.title %></a></strong><br />
      <%= article.content %>
    </li>
  <% end %>
</ul>
